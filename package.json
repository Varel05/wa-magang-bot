{
  "name": "wa-magang-bot-vercel",
  "version": "1.0.0",
  "description": "Chatbot WhatsApp FAQ magang, deploy di Vercel, AI langsung pakai Claude API",
  "type": "module",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -i ./public/admin/input.css -o ./public/admin/style.css --minify",
    "watch:css": "tailwindcss -i ./public/admin/input.css -o ./public/admin/style.css --watch"
  },
  "dependencies": {
    "@google/genai": "^2.16.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
